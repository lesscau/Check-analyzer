<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="MaterialTheme.Light.DarkToolbar">
        <item name="colorPrimary">@color/material_drawer_primary</item>
        <item name="colorPrimaryDark">@color/material_drawer_primary_dark</item>
        <item name="colorAccent">@color/material_drawer_accent</item>
        <item name="android:textColorPrimary">@color/material_drawer_primary_text</item>
        <item name="buttonStyle">@style/Widget.AppCompat.Button.Colored</item>
    </style>

    <style name="AppTheme.Picker" parent="MaterialTheme.Light">
        <item name="android:textColorPrimary">@android:color/black</item>
    </style>

</resources>
